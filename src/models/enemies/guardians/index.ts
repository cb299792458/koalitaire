/**
 * Guardian enemy classes belong in this folder (final act sub-bosses).
 * Import them from `../index.ts` and wire `GUARDIAN_ENCOUNTER_ENEMIES` when you add a guardian.
 *
 * From a file here, import `Enemy` and helpers from `../../Enemy` (not `../Enemy`).
 */

export {};
