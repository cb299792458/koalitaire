/**
 * Boss enemy classes belong in this folder.
 * Import them from `../index.ts` and wire scenario pools (e.g. `makeScenario`) when you add a boss.
 *
 * From a file here, import `Enemy` and helpers from `../../Enemy` (not `../Enemy`).
 */

export {};
